import { Products } from 'src/app/models-interfaces/producs';
export interface Places{
    id : string;
    nombre: string;
    ubicacion: string;
}
export interface Icons{
    icon: string;
    nombre:string;
}